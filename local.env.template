# Local env template — copy values into .env.local
# WARNING: `vercel env pull` OVERWRITES .env.local. Keep your own vars in a backup or re-add after pull.

# From Vercel (auto via `vercel env pull` or `vercel dev`)
VERCEL_OIDC_TOKEN=
BLOB_STORE_ID=store_YVCtk4MMv9CWwVed
# Optional: for local Blob uploads (private store — images served via /api/blob)
# BLOB_READ_WRITE_TOKEN=

# From Neon dashboard
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# Admin
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ekkebar
AUTH_SECRET=your-random-secret
